<UserControl x:Class="FrameViewAnalyzer.App.Views.SessionChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF">
    <Grid>
        <sp:WpfPlot x:Name="ChartHost" />
        <Border x:Name="TooltipOverlay"
                Visibility="Collapsed"
                IsHitTestVisible="False"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="{DynamicResource TooltipBackgroundBrush}"
                BorderBrush="{DynamicResource TooltipBorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10,6">
            <TextBlock x:Name="TooltipText"
                       Foreground="{DynamicResource TextBrush}"
                       FontSize="{DynamicResource FontSize.Small}" />
        </Border>
    </Grid>
</UserControl>
